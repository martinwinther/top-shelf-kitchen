{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$comment": "Build-time feature flags. Keep in sync with site.ts. This file is read by scripts/build.mjs to avoid brittle regex parsing of TypeScript.",
  "search": {
    "enabled": true
  }
}

